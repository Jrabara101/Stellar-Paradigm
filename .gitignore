# Dependencies (loaded from CDN at runtime, not needed in repo)
node_modules/

# Rust / Soroban contract build artifacts
word-scramble-contract/target/
**/*.rs.bk

# OS / editor cruft
.DS_Store
Thumbs.db
.claude/

# Logs
*.log

# Hosted externally (Google Drive) — exceeds GitHub's 100MB per-file push limit
Video/Level 4 Video.mp4

# Cloudflare Worker local dev cache (regenerated by wrangler dev)
.wrangler/
worker/.wrangler/

# Capacitor Android build (generated from root web files; see www/README or capacitor.config.json)
www/

# Android (Capacitor native project) — build output / local machine config only
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/.idea/
android/**/*.iml
*.apk
*.aab
.vercel
