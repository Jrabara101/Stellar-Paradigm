# Dependencies — not needed at runtime (SDK + Wallets Kit load from esm.sh CDN)
node_modules/

# Build / package metadata
package.json
package-lock.json

# Soroban contract source (already deployed on-chain)
word-scramble-contract/

# Documentation / submission assets — not needed at runtime (~87MB)
Video/
Word Scramble Boards Inspired/
Test Mobile UI/
Idea Submissions/
screenshots/
*.docx

# Tooling / editor config
.claude/
.git/
.gitignore

# OS cruft
.DS_Store
Thumbs.db
